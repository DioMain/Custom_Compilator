число Произведение(число а, число б){
	вернуть а * б;
}

пусто Тест() { }

Вход {
	число а = 12;
	число б = 20 + (2 * 2);
	число с = а + б;
	
	Если (а == б && (б != а || 12 > 7)) {
		Тест();
	}
	Иначе Если (а == б) {
		Тест();
	}
	Иначе {
		Тест();
	}
	
	Произведение(а, б);
}